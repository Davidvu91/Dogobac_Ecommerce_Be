# FIRST Step to Create New Server with Express-generator

- npx express-generator --no-view
- npm install

## Install more dependency

- npm i nodemon dotenv cors mongoose jsonwebtoken bcrypt

## Run the Server:

- npm run dev
